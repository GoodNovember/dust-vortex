import React from 'react'

const Tester = ({ id, description, title }) => (
  <div>
    <div>[Tester Element]</div>
    <div>ID:{ id }</div>
    <div>Title:{ title }</div>
    <div>Description:{ description }</div>
  </div>
)

export default Tester
